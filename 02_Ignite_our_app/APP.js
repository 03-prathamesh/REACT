import React from "react"
import ReactDOM from "react-dom"

const heading=React.createElement("h1",{},"First react Code");
const root=ReactDOM.createRoot(document.getElementById("root"));
root.render(heading);